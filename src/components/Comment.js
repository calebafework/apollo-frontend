// import React, {useState} from 'react';

const Comment = (props) => {
    // let useState = (null)
    
    return (
        <div>
            <p>{props.content}</p>
        </div>
    );
}

export default Comment;
